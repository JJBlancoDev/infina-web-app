---
import { Image } from "astro:assets";
import constelation from "../assets/constelacion.svg";
import Button from "./Buttons.astro";
import Menu from "./Menu.astro";
---

<style>
  .rotating-bg {
    animation: slowRotate 80s linear infinite;
  }

  @keyframes slowRotate {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }
</style>

<section
  class="w-full min-h-screen flex flex-col items-center relative pt-24 sm:pt-28 px-4"
>
  <Menu />

  <div
    class="flex flex-col justify-center items-center w-full max-w-5xl min-h-[60vh] relative gap-4 sm:gap-6 py-10"
  >
    <Image
      src={constelation}
      alt="background"
      class="rotating-bg absolute inset-0 w-full h-full max-w-6xl mx-auto object-contain -z-10 opacity-40"
      loading="eager"
    />
    <h3
      class="text-primary font-semibold text-3xl sm:text-4xl md:text-5xl lg:text-[91px] text-center leading-tight"
    >
      Transforma, conecta y potencia tu negocio
    </h3>
    <div class="flex flex-col justify-center items-center gap-2 mt-2">
      <p
        class="text-primary font-semibold text-lg sm:text-xl md:text-2xl text-center"
      >
        Creamos soluciones digitales que multiplican tu impacto
      </p>
      <p class="text-primary text-base sm:text-lg md:text-xl text-center">
        Diseño UX/UI | Desarrollo Web & E-commerce | Software Punto de Venta
        (POS)
      </p>
    </div>

    <div class="mt-4">
      <Button>Solicitar demo</Button>
    </div>
  </div>
</section>
